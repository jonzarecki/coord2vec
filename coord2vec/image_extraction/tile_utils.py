from typing import Tuple

from PIL import Image


def is_tile_empty(im: Image) -> bool:
    pass


def build_tile_extent(center: list, radius_in_meters: float) -> list:
    pass


def sample_coordinate_in_range(min_lat: float, min_lon: float, max_lat: float, max_lon: float) -> Tuple[float, float]:
    pass

